<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container">
    <a class="navbar-brand" href="#">Airbnb </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
      aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto">
        <li class="nav-item">
          <a class="nav-link" href="<%= @flats_path %>">Home</a>
        </li>
      </ul>

       <%= form_with url: flats_path, method: :get, class: "d-flex" do %>
      <%= text_field_tag :query,
            params[:query],
            class: "form-control",
            placeholder: "Search for"
        %>
      <%= submit_tag "Search", name: "", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>
    </form>

      <a href="<%= new_flat_path %>" class="btn btn-primary ml-3">Create New Listing</a>
    </div>
  </div>
</nav>


<div class="container mt-3">
  <div class="row row-cols-1 row-cols-lg-3 row-cols-md-2 g-4">
    <% @flats.each do |flat| %>
      <div class="col">
        <div class="card h-100">
          <img src="<%= flat.image %>" class="card-img-top" alt="...">
          <div class="card-body">
            <h5 class="card-title"><%= flat.name %></h5>
            <p class="card-text"><%= flat.description %></p>
            <div class="d-flex justify-content-between">
              <p class="card-text"><%= flat.price_per_night %>$</p>
              <p class="card-text"><%= flat.number_of_guests %> Guests</p>

            </div>
              <%= link_to 'Delete', flat_path(flat), method: :delete, data: { "turbo-method": :delete }, class: 'btn btn-danger' %>

            <a href="<%= flat_path(flat) %>" class="btn btn-primary" style="text-decoration: none;">View Details</a>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>
