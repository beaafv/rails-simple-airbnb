<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container">
    <a class="navbar-brand" href="#">Airbnb 🛀🏼</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
      aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto">
        <li class="nav-item">
          <a class="nav-link" href="<%= @flats_path %>">Home</a>
        </li>
      </ul>

       <%= form_with url: flats_path, method: :get, class: "d-flex" do %>
      <%= text_field_tag :query,
            params[:query],
            class: "form-control",
            placeholder: "Search for"
        %>
      <%= submit_tag "Search", name: "", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>
    </form>

      <a href="<%= new_flat_path %>" class="btn btn-primary ml-3">Create New Listing</a>
    </div>
  </div>
</nav>

<div class="container">
  <div class="row justify-content-center">
    <div class="col-lg-6">
      <div class="mb-4">
        <img src="<%= @flat.image %>" class="card-img-top" alt="Flat Image">
      </div>
    </div>
    <div class="col-lg-6">
      <div>
        <div>
          <h5><%= @flat.name %></h5>
          <p class="card-text"><%= @flat.description %></p>
          <div class="d-flex justify-content-between">
            <p><%= @flat.price_per_night %>$</p>
            <p><%= @flat.number_of_guests %> Guests</p>
          </div>
          <a href="<%= flats_path %>" class="btn btn-primary" style="text-decoration: none;">Back to 🛀🏻</a>
        </div>
      </div>
    </div>
  </div>
</div>
